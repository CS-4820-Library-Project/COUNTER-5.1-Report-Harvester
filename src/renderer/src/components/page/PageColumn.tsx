import styled from "@emotion/styled";
import { FlexColumnStart } from "../flex";

const PageColumn = styled(FlexColumnStart)(() => ({
  height: "100%",
  gap: "15px",
}));

export default PageColumn;
